{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.13.0"
  },
  "scripts": {
    "compile": "npx hardhat compile",
    "start": "npx hardhat node ",
    "test": "npx hardhat test --network localhost test/FairAuction.test.ts",
    "testnet": "npx hardhat run test/FairAuction_testnet.ts"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.2",
    "dotenv": "^16.0.3"
  }
}
